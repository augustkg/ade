# ADE

- **GitHub repo:** `augustkg/ade` (https://github.com/augustkg/ade)
- **GitHub Project:** ADE (project #2, owner `augustkg`) — `gh project view 2 --owner augustkg`
- **Auth account for `gh`:** `augustkg`

This is a Rust TUI (Cargo workspace, see `Cargo.toml`). For tracker work,
use `gh` against `augustkg/ade` and project #2.
